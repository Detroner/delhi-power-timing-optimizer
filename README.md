# Delhi Power Timing Optimizer

A local Python web app that recommends when to run a load in Delhi by combining Delhi SLDC frequency data and IEX DAM forecasts.

## Setup
pip install -r requirements.txt
python run_app.py

Visit: http://127.0.0.1:8000